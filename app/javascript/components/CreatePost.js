import React from "react"

class CreatePost extends React.Component {
  render () {
    return (
      <React.Fragment>
        {this.props.value}
      </React.Fragment>
    );
  }
}

export default CreatePost